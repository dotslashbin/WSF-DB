CREATE TABLE [dbo].[pickProvenance] (
    [idN]         INT            IDENTITY (1, 1) NOT NULL,
    [displayName] NVARCHAR (100) NULL,
    CONSTRAINT [PK_pickProvenance] PRIMARY KEY CLUSTERED ([idN] ASC)
);

