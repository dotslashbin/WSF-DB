CREATE TABLE [dbo].[pickBottleSize] (
    [idN]         INT            IDENTITY (1, 1) NOT NULL,
    [displayName] NVARCHAR (100) NULL,
    CONSTRAINT [PK_pickbottleSize] PRIMARY KEY CLUSTERED ([idN] ASC)
);

