CREATE TABLE [dbo].[pickMaturity] (
    [idN]         INT            IDENTITY (1, 1) NOT NULL,
    [displayName] NVARCHAR (100) NULL,
    CONSTRAINT [PK_pickMaturity] PRIMARY KEY CLUSTERED ([idN] ASC)
);

